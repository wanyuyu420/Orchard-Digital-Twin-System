# 赣南脐橙果园数字孪生系统 · 作品介绍文档

> 参赛方向：易智瑞（GeoScene）地理信息系统开发竞赛
> 技术底座：GeoScene Enterprise（Server + Portal 联合部署）+ GeoAI 深度学习 + 三维数字孪生

---

## 一、作品概述

赣南脐橙果园数字孪生系统，是一套面向赣南脐橙产业的果园数字化管理平台。系统以 **GeoScene Enterprise** 为 GIS 核心架构，融合**无人机遥感、GeoAI 深度学习（目标检测 + 分割大模型 + 冠层高度反演）、三维实景建模（CityEngine）与 CesiumJS 三维可视化**，构建了从「无人机影像采集 → AI 树冠智能分割 → 三维数字孪生建模 → 生长态势诊断 → 变量施肥决策」的全流程闭环。

系统的核心价值在于：让上万棵果树的「家底」从模糊走向精确，让「凭经验巡园、凭感觉施肥」的传统粗放管理，升级为「逐棵识别、量化诊断、精准施肥」的数字化精细管理。

本作品的核心立意是：**让果园里的每一棵树，都成为一个可识别、可量化、可查询、可决策的「空间要素」**。传统果园管理长期停留在经验驱动的粗放模式，本系统通过 AI 自动识别与 GeoScene 空间服务，把模糊的果园变成精确的数字资产。

---

## 二、需求分析

### 2.1 行业背景与问题提出

赣南是我国最大的脐橙产区，脐橙产业是当地农业支柱与乡村振兴的重要抓手。然而传统果园管理长期面临三大痛点：

1. **果树底数不清**：一个果园动辄上万棵果树，人工逐棵清点、测量树冠面积与树高几乎不可能，「家底」长期模糊。
2. **长势监测靠经验**：果农凭借肉眼与经验判断树势强弱，缺乏客观、量化的生长指标，弱树、病树难以被及时发现。
3. **施肥「一刀切」**：传统施肥不分树势强弱均匀撒施，造成肥料浪费、成本上升——弱树补不上、强树施过量。

**问题的本质**：缺乏一套「以空间要素为核心、以 AI 为手段、以三维为表达」的果园精细化管理工具。

### 2.2 需求定义

针对上述痛点，系统需实现以下能力：

| 需求 | 说明 |
|---|---|
| 自动化果树识别 | 从无人机正射影像中自动识别每棵果树的位置与树冠轮廓 |
| 生长指标反演 | 量化单株的树冠面积、树高、体积、生长健康指数 |
| 三维可视化 | 在真实地理环境中三维呈现果园与逐棵果树 |
| 精准决策 | 基于生长指数给出差异化变量施肥建议 |
| 空间查询分析 | 支持框选区域进行批量统计与诊断 |

### 2.3 技术路线选择（为什么这么设计）

- **数据源选无人机正射影像而非地面测量**：无人机可低成本、大范围、高频次获取果园影像，是「大数据」能力的入口；正射影像带空间参考，天然可入 GIS。
- **识别选「YOLO 检测 + SAM 分割」双阶段**：YOLO 先粗定位包围框，SAM 再精分割轮廓，兼顾速度与精度，适配高分辨率大影像。
- **GIS 核心选 GeoScene Enterprise**：果树本质是空间要素，其管理、查询、入库必须依托专业的 GIS 服务器，而非自建空间引擎。
- **表达选三维数字孪生**：CityEngine 建模 + Cesium 渲染，让管理者「所见即所得」地俯瞰果园全貌。

### 2.4 应用价值与推广前景

- **经济效益**：变量施肥可显著降低肥料成本，精准定位弱树可减少减产损失。
- **社会效益**：推动赣南脐橙产业数字化、智能化升级，助力乡村振兴。
- **推广价值**：「无人机 + AI + GIS」的技术路线具备强可复制性，可横向推广至柑橘、苹果、梨等经济林果乃至林业、农业园区的数字化管理。

---

## 三、系统架构说明（详细说明每块功能使用的开发工具）

### 3.1 总体架构

系统采用 B/S 架构，自下而上分为数据层、GIS 核心层、应用服务层、表现层四层：

```
表现层（前端）
  Vue3 + TypeScript + CesiumJS + Element Plus + ECharts
  （三维场景渲染、交互、图表看板）

应用服务层（后端）
  FastAPI（业务调度 / REST API）
  ├─ GeoAI 推理服务（YOLOv8 检测 + MobileSAM 分割 + 冠层高度反演）
  └─ GeoSceneService（FeatureServer REST 客户端）

GIS 核心层
  GeoScene Enterprise
  ├─ GeoScene Server（FeatureServer 空间服务）
  ├─ GeoScene Portal（服务发布 / 门户管理）
  ├─ GeoScene Pro（影像处理 / SAM 分割 / 制图）
  └─ CityEngine（规则三维建模）

数据层
  PostgreSQL + PostGIS（空间数据库）
  + 3D 静态数据（GLB / 3D Tiles / DEM）
```

### 3.2 各功能模块与开发工具对照

| 功能模块 | 功能说明 | 开发工具/技术 | 是否 GIS 核心 |
|---|---|---|---|
| 空间数据管理 | 果树要素的存储与发布 | GeoScene Server FeatureServer | 是（核心） |
| 空间查询（拉框找树） | 框选区域批量统计诊断 | GeoScene FeatureServer REST `/query` | 是（核心） |
| 要素写入（AI 结果入库） | AI 识别结果写回服务 | GeoScene FeatureServer `/applyEdits` | 是（核心） |
| 三维建模 | 果园与果树三维模型生成 | GeoScene Pro + CityEngine | 是（核心） |
| AI 模型接入 | 树冠检测/分割/树高反演 | YOLOv8 + MobileSAM + GeoAI（PyTorch） | 否（辅助） |
| 前端三维渲染 | 三维场景展示与交互 | CesiumJS + Vue3 | 否（辅助） |
| 业务逻辑调度 | API 编排、异步任务 | FastAPI | 否（辅助） |
| 底层数据存储 | 属性/空间数据持久化 | PostgreSQL + PostGIS | 否（辅助） |

### 3.3 关键技术详解

#### （1）GIS 核心层 —— GeoScene Enterprise

- **Server + Portal 联合部署**：系统采用 GeoScene Server 与 GeoScene Portal 联合部署方式，通过 Portal 完成 FeatureServer 服务的发布与门户管理，Server 对外提供标准 REST 服务。
- **FeatureServer**：发布 `OrangeTrees` 果树要素服务，承载全部果树空间数据。
- **空间查询**：后端通过 FeatureServer REST API 的 `/query` 接口，传入前端框选的 WGS84 多边形，以 `esriSpatialRelContains`（包含关系）执行空间相交查询，返回框内果树要素并聚合统计指标。
- **要素写入**：AI 识别出的新果树通过 `/applyEdits` 接口批量写入 FeatureServer，实现「AI 结果入库」的 GIS 化。
- **GeoScene Pro**：用于无人机正射影像预处理、SAM 大模型树冠分割、成果制图与 SHP 导出。
- **CityEngine**：基于分割后的树冠 SHP 数据，通过规则建模批量生成果园与果树三维模型（GLB / 3D Tiles）。

#### （2）AI 层 —— GeoAI 深度学习

- **YOLOv8**：树冠目标检测，从无人机正射影像中快速定位每棵果树的包围框。
- **MobileSAM**：以 YOLO 输出的包围框为 Prompt 的轻量级分割大模型，精确抠出每棵树的树冠轮廓，输出分割掩膜与置信度。
- **冠层高度反演模型**：基于自监督大模型的冠层高度回归，从 RGB 影像反演逐像素树高，进一步提取单株树高。
- **地形微气候**：结合高分辨率 DSM 与 SRTM，计算每棵树的坡度、坡向，用于长势影响因素分析。
- **生长指数模型**：综合树冠紧凑度、高冠比等指标，构建 0-1 生长健康指数，并映射为变量施肥等级（轻/中/重）。

#### （3）前端可视化层 —— CesiumJS

- CesiumJS 负责三维场景渲染与交互，作为「数据可视化层」消费 GeoScene 服务成果与 CityEngine 模型，不承担 GIS 核心运算。

### 3.4 数据流与调用时序

系统存在两条核心数据管线，共同构成「建模—识别—入库—诊断」的完整闭环：

**管线一：离线三维建模（一次性，构建数字底座）**

```
无人机正射 TIF → GeoScene Pro + SAM 分割树冠 → 导出树冠 SHP → CityEngine 规则建模 → GLB / 3D Tiles → Cesium 三维渲染
```

**管线二：在线 AI 识别与空间诊断（持续，动态更新）**

```
1) 前端上传 TIF → 后端 FastAPI 接收
2) 后端执行 YOLO 检测 + MobileSAM 分割 + GeoAI 冠层高度反演
3) AI 结果 applyEdits 批量写入 GeoScene FeatureServer（空间要素入库）
4) 前端拉框 → 后端 /query 空间相交查询 GeoScene FeatureServer
5) 返回框内果树要素 → 前端 Cesium 渲染 + 统计看板
```

管线二严格以 GeoScene FeatureServer 为空间数据的中枢：AI 结果必须经 `applyEdits` 写入、空间查询必须经 `/query` 读取，系统离开 GeoScene 无法完成任何空间操作。

---

## 四、总体设计

### 4.1 功能设计

| 序号 | 功能模块 | 功能描述 |
|---|---|---|
| 1 | 三维大屏总览 | 开屏加载历史老树全量数据 + CityEngine 果园精模，展示果园全貌 |
| 2 | 拉框空间诊断 | 鼠标在三维场景框选区域，实时统计框内果树数量、平均树高、平均冠幅、平均生长指数、变量施肥建议分布 |
| 3 | 精细查询 | 按健康状态（健康/预警/危急）过滤全量果树，定位异常个体 |
| 4 | TIF 上传与智能分割 | 上传无人机正射 TIF，后台异步执行 YOLO + SAM 分割，进度实时回传 |
| 5 | 变量施肥决策 | 基于生长指数自动生成差异化施肥建议，可视化施肥分区 |
| 6 | 图层管理 | 数据驱动的图层配置（3D Tiles / 点 / WMS / GeoJSON），支持显隐、排序、分组 |
| 7 | 三维测量工具 | 距离、面积、剖面、体积量算，BIM 对齐、地形压平 |

### 4.2 数据库设计

系统采用 **PostgreSQL + PostGIS** 作为空间数据库，核心表设计如下：

**orange_trees（果树要素表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer | 主键 |
| batch_id | String | 数据批次（历史示范区 / 上传时间戳） |
| geom | Geometry(POINT, 32650) | 树冠中心点空间要素（UTM 50N） |
| confidence | Float | SAM 分割置信度 |
| compactness | Float | 树冠紧凑度 |
| shape_length | Float | 树冠边界周长（米） |
| shape_area | Float | 树冠面积（平方米） |
| value / count | Float | 原始栅格像素值 / 像素计数 |
| area_m2 | Float | 树冠投影面积 |
| height_m | Float | 反演树高 |
| crown_diameter | Float | 冠幅直径 |
| volume_m3 | Float | 三维材积 |
| growth_index | Float | 生长健康指数 |
| slope_degree / aspect | Float | 坡度 / 坡向 |
| fertilizer_level | Integer | 变量施肥等级（0-3） |

**gis_layers（图层配置表）**：图层编码、名称、分组、类型（3dtiles / api_point / wms / geojson）、数据源 URL、显隐状态、配置 JSON、排序。

**设计要点**：

- **空间参考对齐**：空间字段采用 PostGIS Geometry 类型、SRID 32650（UTM 50N 投影），与 GeoScene FeatureServer 的空间参考保持一致，保证空间查询与要素写入的无缝衔接。
- **批次隔离**：通过 `batch_id` 区分「历史示范区」与「每次上传」，支持多次无人机航拍成果的增量入库与独立管理。
- **空间索引**：`geom` 字段建立空间索引，支撑万级果树要素的高效空间查询。
- **决策字段下沉**：将 `growth_index`（生长指数）、`fertilizer_level`（施肥等级）等分析结果直接落到要素属性，使「逐棵树」的精准决策成为可能。

### 4.3 关键技术

| 关键技术 | 技术方案 | 为什么这么选 |
|---|---|---|
| GeoAI 树冠分割 | YOLOv8 + MobileSAM 双阶段检测-分割 | YOLO 先粗定位包围框、SAM 再精分割轮廓，用包围框 Prompt 降低分割计算量，兼顾速度与精度，适配无人机大影像 |
| 冠层高度反演 | 自监督大模型（SSLhuge）单目影像高度回归 | 无需 LiDAR 等昂贵设备，仅凭 RGB 正射影像即可反演逐株树高，降低数据采集门槛 |
| GeoScene 空间服务 | FeatureServer `/query` + `/applyEdits` | 果树作为空间要素，其查询与写入依托专业 GIS 服务器，保证空间运算的严谨性与可扩展性 |
| 三维建模 | CityEngine SHP 驱动规则建模 | 以分割树冠 SHP 为输入，通过规则批量生成上万棵果树模型，效率远高于人工建模 |
| 三维可视化 | CesiumJS GLB + 3D Tiles 融合渲染 | 开源、高性能的 Web 三维引擎，作为展示层消费 GeoScene 服务成果，轻量接入 |

以上关键技术的选择均遵循一条主线：**AI 负责「看得懂」，GeoScene 负责「管得住、查得准」，三维负责「表达得清」**，各层职责分明、松耦合。

---

## 五、作品亮点

### 5.1 构思新颖

- 将「大模型分割（SAM）+ 三维数字孪生（CityEngine + Cesium）+ 空间分析（GeoScene）」深度融合，形成「AI 识别 → 三维建模 → 空间诊断 → 精准施肥」的完整闭环，在农业 GIS 领域具备较强新颖性。

### 5.2 方法巧妙

- **SAM 大模型接入 GeoScene Pro**：在 GeoScene Pro 中接入 SAM 大模型完成树冠分割，直接产出可制图、可入库的 SHP 成果，实现 AI 与 GIS 的无缝衔接（而非割裂的两套工具）。
- **双阶段检测-分割**：YOLO 先粗定位、SAM 后精分割，用包围框 Prompt 降低分割计算量，适配无人机大影像。
- **轻量化部署**：MobileSAM 轻量化模型 + 推理后释放显存策略，让大模型推理在普通服务器上可运行。

### 5.3 GIS 技术应用

- **空间思维体现**：以「果树 = 空间要素」为核心，贯穿识别（点/多边形）、存储（PostGIS/FeatureServer）、查询（空间相交）、分析（坡度坡向地形因子）、可视化（三维）全过程。
- **三维 + 遥感 + 深度学习融合**：三维（CityEngine/Cesium）、遥感（无人机正射 + DSM + SRTM）、深度学习（YOLO/SAM/GeoAI）三类新技术在本作品中的综合应用。
- **GeoScene 核心作用**：空间数据管理、空间查询、要素写入、三维建模全部依托 GeoScene Enterprise，GIS 技术是系统的中枢。

### 5.4 新技术融合（大数据 / 云计算 / 无人机 / GEOAI）

- **大数据**：无人机高分辨率正射影像（GB 级 TIF）的切片处理、万级果树要素的存储与空间查询，体现大数据处理能力。
- **云计算**：云原生 B/S 架构、前后端分离，GeoScene Enterprise 服务化部署 + 后端 API 服务 + 静态数据服务分布式部署，可弹性迁移至云服务器。
- **无人机**：以无人机正射影像为主要数据源，结合 DSM 高程，实现空地一体数据采集。
- **GEOAI**：深度学习模型与 GIS 平台的深度融合，是平台新产品技术的深入应用。

---

## 六、GeoScene 合规性说明（逐条判定）

依据竞赛规则「GIS 核心架构/功能必须采用 GeoScene Enterprise/Runtime SDK/Web API，允许部分功能使用开源，但服务器端部署不得完全脱离 GeoScene」逐条判定：

**第一条：GIS 核心架构/功能采用 GeoScene Enterprise —— 满足**

| GIS 核心功能 | 采用技术 | 判定 |
|---|---|---|
| 空间数据管理 | GeoScene Enterprise（FeatureServer） | 核心 |
| 空间查询（拉框找树） | GeoScene FeatureServer REST `/query` | 核心 |
| 要素写入（AI 结果入库） | GeoScene FeatureServer `/applyEdits` | 核心 |
| 三维建模 | CityEngine + GeoScene Pro | 核心 |
| 前端三维渲染 | CesiumJS（开源） | 可视化层，非核心 |

CesiumJS 是数据可视化层，不承担 GIS 核心运算；GIS 核心运算全部在 GeoScene 体系内完成，Cesium 仅消费 GeoScene 服务做画面呈现，属于「数据可视化」，不构成核心 GIS 功能。

**第二条：允许部分功能使用开源 —— 满足**

| 开源组件 | 用途 | 是否核心 GIS |
|---|---|---|
| FastAPI | 业务逻辑调度 | 否 |
| CesiumJS | 前端 3D 可视化 | 否 |
| PostgreSQL / PostGIS | 底层数据存储 | 否 |
| YOLO / SAM / PyTorch | AI 推理 | 否 |

以上开源组件全部为辅助角色，不替代 GeoScene 的 GIS 核心地位。

**第三条：服务器端不脱离 GeoScene —— 满足**

| 验证项 | 结果 |
|---|---|
| 启动时校验 GeoScene 连通性 | 不通则拒绝启动（sys.exit） |
| 缺少 GeoScene 配置 | 拒绝启动 |
| 运行时 GeoScene 故障 | 空间查询返回 HTTP 503，无 fallback |
| 所有空间查询 | 必须经过 FeatureServer |
| 直连数据库绕过路径 | 已删除 |

**最终结论**：系统离开 GeoScene 无法运行。CesiumJS 是展示层而非 GIS 核心，GIS 核心运算全部在 GeoScene 体系内完成，满足竞赛合规要求。

---

## 七、系统安装与配置

- **后端**：conda 环境（Python 3.10 + torch 2.5.1 + CUDA 12.1），安装 `requirements.txt`，配置 `.env`（GeoScene Server 地址/账号、数据库连接）。
- **前端**：`npm install && npm run dev`。
- **GeoScene**：GeoScene Enterprise（Server + Portal 联合部署）发布 OrangeTrees FeatureServer。
- **静态 3D 数据服务**：`python scripts/serve_orchard.py` 提供 GLB / 3D Tiles / DEM。

---

## 八、人员信息

（姓名 / 学校 / 分工 —— 待补充）
