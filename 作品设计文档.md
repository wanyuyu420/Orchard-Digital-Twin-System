# 赣南脐橙果园数字孪生系统 · 作品设计文档

> 配套《作品介绍文档》，面向技术评审，详述系统总体设计、接口设计、数据库设计、核心算法与模块设计。

---

## 一、系统总体设计

### 1.1 架构分层

系统采用前后端分离的 B/S 架构，自上而下分为四层：

| 层次 | 职责 | 技术实现 |
|---|---|---|
| 表现层 | 三维场景渲染、交互、图表看板 | Vue3 + TypeScript + CesiumJS + Element Plus + ECharts |
| 应用服务层 | 业务编排、AI 推理、GeoScene 客户端 | FastAPI + PyTorch（YOLOv8/MobileSAM/GeoAI） |
| GIS 核心层 | 空间数据管理、空间查询、要素写入、三维建模 | GeoScene Enterprise（Server + Portal 联合部署）、GeoScene Pro、CityEngine |
| 数据层 | 空间/属性数据持久化 | PostgreSQL + PostGIS + 3D 静态数据（GLB/3D Tiles/DEM） |

### 1.2 技术选型依据

- **GeoScene Enterprise 作为 GIS 核心**：果树本质是空间要素，其管理、查询、入库必须依托专业 GIS 服务器，保证空间运算的严谨性、开放性与可扩展性，同时满足竞赛「GIS 核心架构必须采用 GeoScene」的要求。
- **FastAPI 作为业务层**：轻量、异步、天然支持后台任务，适合编排「AI 推理 + GeoScene 调用」的混合流程。
- **PostgreSQL + PostGIS 作为数据层**：开源空间数据库，与 GeoScene FeatureServer 空间参考对齐，作为服务发布的数据底座。
- **CesiumJS 作为可视化层**：开源 Web 三维引擎，仅消费 GeoScene 服务成果做画面呈现，不承担 GIS 核心运算。

---

## 二、功能模块设计

系统划分为七大功能模块：

| 模块 | 核心功能 | 数据来源 |
|---|---|---|
| 三维大屏总览 | 果园全貌展示、历史老树加载 | FeatureServer（historical_zone）+ CityEngine 精模 |
| 拉框空间诊断 | 框选区域批量统计、变量施肥建议 | FeatureServer `/query` 空间相交 |
| 精细查询 | 按健康状态过滤果树、定位异常个体 | FeatureServer 全量查询 |
| TIF 上传与智能分割 | 无人机影像上传、AI 异步推理 | YOLOv8 + MobileSAM + GeoAI |
| 变量施肥决策 | 生长指数 → 施肥等级 → 分区可视化 | 生长指数模型 |
| 图层管理 | 数据驱动图层配置（显隐/排序/分组） | gis_layers 表 |
| 三维测量工具 | 距离/面积/剖面/体积量算、BIM 对齐 | Cesium 交互 |

---

## 三、接口设计（REST API）

后端统一前缀 `/api/v1`，核心接口如下：

### 3.1 空间诊断接口

| 项 | 内容 |
|---|---|
| 路径 | `POST /api/v1/orange/spatial-diagnose` |
| 请求体 | `{ "coordinates": [[lng, lat], ...] }`（WGS84 闭合多边形） |
| 响应 | `{ total_count, avg_height, avg_area, avg_growth_index, fertilizer_recommendation, trees[] }` |
| 处理流程 | 识别经纬度顺序 → 强制闭合 → GeoScene `/query` 空间相交（esriSpatialRelContains）→ 聚合统计 |

### 3.2 精细查询接口

| 项 | 内容 |
|---|---|
| 路径 | `POST /api/v1/orange/trees/filter` |
| 请求体 | `{ healthStatuses: ["healthy"|"warning"|"critical"], startDate, endDate }` |
| 响应 | 同空间诊断 |
| 处理流程 | 全量查询 FeatureServer → 按生长指数映射健康状态过滤 |

### 3.3 历史老树接口

| 项 | 内容 |
|---|---|
| 路径 | `GET /api/v1/orange/historical-trees` |
| 响应 | `{ total, trees[] }` |
| 处理流程 | FeatureServer 查询 `batch_id='historical_zone'` 全量要素 |

### 3.4 上传与智能分割接口

| 项 | 内容 |
|---|---|
| 路径 | `POST /api/v1/orange/upload-and-interpret`（multipart 文件上传） |
| 响应 | `{ task_id, status: "pending"|"processing"|"completed"|"failed", progress, total_trees, fresh_trees[] }` |
| 任务查询 | `GET /api/v1/orange/upload-and-interpret/{task_id}` |
| 处理流程 | 见「五、核心算法与流程设计」 |

### 3.5 图层管理接口

| 项 | 内容 |
|---|---|
| 路径 | `GET/POST /api/v1/layers`、`GET/PUT/DELETE /api/v1/layers/{id}`、`PATCH /api/v1/layers/{id}/visibility` |
| 功能 | 图层配置 CRUD 与显隐切换 |

---

## 四、数据库设计

### 4.1 ER 关系

```
GISLayer（图层配置） ────── 独立，与果树要素无外键关联
OrangeTree（果树要素） ──── 按 batch_id 分批次，geom 空间字段建索引
```

### 4.2 核心表结构

**orange_trees（果树要素表）**

| 字段 | 类型 | 约束 | 说明 |
|---|---|---|---|
| id | Integer | 主键 | 果树唯一标识 |
| batch_id | String | 索引 | 数据批次（historical_zone / 上传时间戳） |
| geom | Geometry(POINT, 32650) | 非空、空间索引 | 树冠中心点（UTM 50N） |
| confidence | Float | 可空 | SAM 分割置信度 |
| compactness | Float | 可空 | 树冠紧凑度 |
| shape_length | Float | 可空 | 树冠周长（米） |
| shape_area | Float | 可空 | 树冠面积（平方米） |
| value / count | Float | 可空 | 原始栅格像素值 / 像素计数 |
| area_m2 | Float | 可空 | 树冠投影面积 |
| height_m | Float | 可空 | 反演树高 |
| crown_diameter | Float | 可空 | 冠幅直径 |
| volume_m3 | Float | 可空 | 三维材积 |
| growth_index | Float | 可空 | 生长健康指数（0-1） |
| slope_degree / aspect | Float | 可空 | 坡度 / 坡向 |
| fertilizer_level | Integer | 非空默认 0 | 施肥等级（0 未计算 / 1 轻 / 2 中 / 3 重） |

**gis_layers（图层配置表）**

| 字段 | 类型 | 说明 |
|---|---|---|
| id | Integer | 主键 |
| code | String(64) | 唯一图层编码 |
| name | String(128) | 图层名称 |
| group_name | String(64) | 分组 |
| layer_type | String(32) | 类型（3dtiles/api_point/wms/geojson） |
| url | String(512) | 数据源 URL |
| is_visible / is_enabled | Boolean | 显隐 / 启用状态 |
| config | JSON | 灵活配置 |
| order | Integer | 显示顺序 |

---

## 五、核心算法与流程设计

### 5.1 树冠检测-分割双阶段流程

```
输入：无人机正射 TIF
  ↓
分块切片（TifService，带重叠 112px）
  ↓
智能跳过空白/低对比度块（max<10 或 std<5）
  ↓
YOLOv8 树冠检测（conf=0.135, iou=0.7）→ 包围框 + 外扩 padding
  ↓
MobileSAM Box Prompt 分割（multimask_output=False）
  ↓
有效掩膜过滤（与 valid_mask 交集占比 <0.8 则剔除）
  ↓
最大外轮廓提取 → Douglas-Peucker 简化（保留 99.5% 周长精度）
  ↓
像素坐标 → 地理坐标（rasterio transform + WGS84 投影）
```

### 5.2 生长指标反演

**冠层面积与冠幅**：

- `area_m2 = area_px × GSD²`（GSD 为影像地面分辨率）
- `crown_diameter = 2 × √(area_m2 / π)`
- `compactness = 4π × area_px / perimeter²`（越接近 1 越圆整）

**冠层高度**：GeoAI 自监督大模型（compressed_SSLhuge_aerial）从 RGB 回归逐像素高度，取树冠掩膜内中位数作为单株树高。

**生长健康指数**：

```
hc_ratio = height_m / crown_diameter
hc_score = max(0, 1 - |hc_ratio - 1|)        # 高冠比越接近 1 越健康
growth_index = 0.5 × compactness + 0.5 × hc_score
```

**变量施肥等级**：

```
growth_index ≥ 0.7  → 施肥等级 1（轻度）
0.4 ≤ growth_index < 0.7 → 施肥等级 2（中度）
growth_index < 0.4  → 施肥等级 3（重度）
```

### 5.3 地形微气候

结合高分辨率 DSM（默认）与 SRTM（回退），通过中心差分计算坡度坡向：

```
dz/dy = (z_north - z_south) / (2×res)
dz/dx = (z_east - z_west) / (2×res)
slope = atan(√(dz/dx² + dz/dy²))
aspect = atan2(-dz/dx, -dz/dy)
```

### 5.4 空间诊断流程

```
前端 Cesium 框选 → 多边形坐标（WGS84）
  ↓
后端识别经纬度顺序、强制闭合
  ↓
GeoScene FeatureServer /query（esriSpatialRelContains）
  ↓
聚合统计：总数、平均树高、平均冠幅、平均生长指数、施肥等级分布
  ↓
返回前端 → 看板渲染 + 三维高亮
```

---

## 六、核心类 / 模块设计

### 6.1 后端服务类

| 类 | 职责 | 关键方法 |
|---|---|---|
| GeoSceneService | GeoScene FeatureServer REST 客户端（单例 + Token 缓存） | `get_token`、`health_check`、`query_features`、`query_stats`、`add_features` |
| YoloService | YOLOv8 树冠检测模型加载与推理 | `get_instance`、`detect_boxes` |
| SamInferenceService | MobileSAM 分割模型加载与推理 | `get_instance`、`infer_tile_with_boxes` |
| HeightService | GeoAI 冠层高度反演 | `get_instance`、`predict_height_map`、`release` |
| ElevationService | DSM/SRTM 坡度坡向计算 | `get_slope_aspect` |
| TifService | TIF 分块切片 | `slice_tif_generator` |

### 6.2 前端组件

| 组件 | 职责 |
|---|---|
| OrchardDashboard | 三维大屏总览 |
| CanopyAnalysis | 树冠分析 |
| AgriDecision | 农业决策（变量施肥） |
| OrchardControlBar | 底部控制条（树透明度/显隐/精模懒加载） |
| QueryPanel / QueryResultPanel | 查询交互与结果展示 |
| FertilizationWindow | 施肥建议窗口 |
| GISLayer / DynamicLayerRenderer | 数据驱动图层渲染 |

---

## 七、关键技术实现细节

### 7.1 GeoScene 集成

- **认证**：`/tokens/generateToken` 获取 Token（requestip 型），内置缓存与过期续期。
- **健康检查**：`/rest/info` + FeatureServer 连通性探测，启动时校验。
- **空间查询**：`/0/query`，geometry 参数以合法 JSON 字符串传递，`esriSpatialRelContains` 相交关系。
- **要素写入**：`/0/applyEdits`，批量 `adds` 提交 AI 识别结果。

### 7.2 坐标转换

- 前端交互与空间查询使用 WGS84（EPSG:4326）。
- 数据库与 FeatureServer 使用 UTM 50N（EPSG:32650，赣州所处投影带）。
- 通过 pyproj Transformer 在两者间转换，保证空间参考一致。

### 7.3 异步任务机制

- 上传接口立即返回 `task_id`，后台线程（asyncio.to_thread）执行 AI 推理。
- 前端轮询任务状态接口获取实时进度，避免长时间阻塞请求。
- 推理完成后释放大模型显存，保证服务持续可用。

---

（完）
